# Full local development environment: the compute stack, the API, and the notebooks.
#
# Deployment uses the two narrower files instead:
#   requirements-compute.txt  — what src/ needs to compute anything
#   requirements-api.txt      — the FastAPI service on top of it

-r requirements-compute.txt
-r requirements-api.txt

# Notebooks (exploratory work under notebook/)
jupyter>=1.0.0
ipykernel>=6.0.0
